{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}

    {# {{ form(formulaire, {"attr": {"novalidate": "novalidate"}}) }} #} 
    {{ form(formulaire) }}

{% endblock %}

